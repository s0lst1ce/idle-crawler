language: rust
script:
  - cd test && cargo build --verbose --all
  - cd test && cargo test --verbose --all
rust:
  - stable
  - beta
  - nightly

jobs:
  allow_failure:
    - rust: nightly
  fast_finish: true
cache: cargo
